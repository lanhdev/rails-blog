<p>
	<%= comment.body %><br>
	<cite><span class="text-muted"> - </span><%= comment.commenter %></cite>
</p>
<p>
	<%= link_to 'Delete', [comment.article, comment], class: 'btn btn-danger',
	method: :delete,
	data: { confirm: 'Are you sure?' } %>
</p>